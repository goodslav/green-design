version: '3'

services:
  app:
    image: deetoo/app:bdmlr-${TAG}
    environment:
      - NODE_ENV=production
    deploy:
      replicas: 3
      restart_policy:
        condition: on-failure
    networks:
      - bdmlr-overlay
networks:
  bdmlr-overlay:
    external:
      name: bdmlr-overlay